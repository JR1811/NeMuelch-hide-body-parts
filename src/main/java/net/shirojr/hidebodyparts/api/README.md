# Hide Body Parts API

## Hide Body Parts dynamically with custom Items

Implement the [BodyPartHider](./BodyPartHider.java) interface on a custom Item class to modify hidden parts dynamically.

All methods have a default implementation with no specified entries, 
so choose which one to override to adjust the hidden parts modification.

---

*(More will be added in the future based on demand)*